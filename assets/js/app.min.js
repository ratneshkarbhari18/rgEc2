$("a#sideNavOpenLink").click(function (e) {
    e.preventDefault();
    $("div#sidenavMobileCloser").css("display", "block");
    $("div#sidenavMobile").css("display", "block");
});

$("div#sidenavMobileCloser").click(function (e) {
    e.preventDefault();
    $(this).css("display", "none");
    $("div#sidenavMobileProductFilter").css("display", "none");
    $("div#sidenavMobile").css("display", "none");
});
$("input#showPwdCustomerLogin").click(function (e) {
    e.preventDefault();
    if ($("input#customer-password").attr("type") == "password") {
        $("input#customer-password").attr("type", "text");
    } else {
        $("input#customer-password").attr("type", "password");
    }
});
$("div#product-gallery-box").owlCarousel({ nav: !1, dots: !0, responsive: { 0: { items: 3 }, 600: { items: 3 }, 1000: { items: 3 } }, margin: 10, navigationText: ["<", ">"], loop: !1 });
var owlSliderHome = $("#home-hero-slider,#home-hero-slider-mobile");
owlSliderHome.owlCarousel({ nav: !1, dots: !0, responsive: { 0: { items: 1 }, 600: { items: 1 }, 1000: { items: 1 } }, loop: !0, autoplay: !0, autoplayTimeout: 3500, autoplayHoverPause: !0 });
$("div#home-category-slider-mobile").owlCarousel({ nav: !0, responsive: { 0: { items: 5 }, 600: { items: 5 }, 1000: { items: 5 } }, margin: 5, loop: !0 });
$("div#daily-deals-carousel,div#related-products").owlCarousel({ dots: !0, responsive: { 0: { items: 2 }, 600: { items: 3 }, 1000: { items: 5 } }, autoplay: !0, autoplayTimeout: 3500, autoplayHoverPause: !0, margin: 10, loop: !0 });

lazyLoadInstance.update();
$(".currency-switcher-item").click(function (e) { 
    e.preventDefault();
    let name = $(this).attr("currency_name");
    let currencySwitchingUrl = $(this).attr("currency_switching_url");
    $.ajax({
        type: "GET",
        url: currencySwitchingUrl,
        data: {
            "name" : name
        },
        success: function (response) {
            if (response=="currency-set") {
                location.reload();
            }
        }
    });
});

function setCookie(name, value, days) {
    var expires = "";
    if (days) {
        var date = new Date();
        date.setTime(date.getTime() + (days * 24 * 60 * 60 * 1000));
        expires = "; expires=" + date.toUTCString();
    }
    document.cookie = name + "=" + (value || "") + expires + "; path=/";
}

function getCookie(name) {
    var nameEQ = name + "=";
    var ca = document.cookie.split(';');
    for (var i = 0; i < ca.length; i++) {
        var c = ca[i];
        while (c.charAt(0) == ' ') c = c.substring(1, c.length);
        if (c.indexOf(nameEQ) == 0) return c.substring(nameEQ.length, c.length);
    }
    return null;
}